pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Example build step (replace with your build tool)
                echo 'Building the project...'
            }
        }

        stage('Test') {
            steps {
                // Example test step
                echo 'Running tests...'
            }
        }

        stage('Deploy') {
            steps {
                // Example deploy step
                echo 'Deploying application...'
            }
        }
    }
}z
